services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-app
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: "postgresql://postgres:PAKAWAN0367@postgres:5432/sadatabase"
    restart: always
    networks:
      - sa-stucu01  # Connect to the existing sa-network


networks:
  default:
    external: true
    name: sa-stucu01
